<h1>All Patients</h1>

<ul>
        <% @patients.each do |patient| %>
            <li>Name: <%= link_to patient.name, patient_path(patient) %></li><br>
            Number of Appointments: <%= patient.appointments.length %>
            <br></br>
        <% end %>
</ul>

